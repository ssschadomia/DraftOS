# desktop/cosmic — COSMIC customization

RON presets shipped as system defaults under `/usr/share/cosmic/<id>/v<N>/<key>`
(each key is its own file). Defines the DraftOS panel, floating dock, theme and
"glass". See docs/decisions/0002-desktop-is-cosmic.md.
