# tools — development helpers

Dev-only scripts (not shipped in any edition).

- `shot.sh` — screenshot a libcosmic app window under XWayland; the reliable capture path on a locked-down GNOME Wayland host (no grim/portal needed). See the header for usage.
